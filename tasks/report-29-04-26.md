 KRİTİK (data loss / sürekli bozuk durum)                                                                              
  K1. TerminalRegistry::shutdown_all() ring buffer'ı flush ETMİYOR ve panes.status/closed_at güncellemiyor             
  src-tauri/src/sidecar/terminal.rs:596-614 — uygulamayı kapatırken sadece child'ları öldürüp panes map'i drain ediyor.
   flush_ring_to_db çağrılmıyor. Normal pane kapanışında run_waiter task'ı bunu yapıyor ama app shutdown'da
  RunEvent::ExitRequested block_on'u dönerken waiter'ın spawn ettiği async flush task'ı çalışma şansı bulamıyor. Sonuç:
   WP-W2-06'nın "ring buffer 5,000 satır persist eder" kabul kriteri en sık tetiklenen yolda (app close) sessizce      
  başarısız. Önceki oturumun scrollback'i kayboluyor, panes satırlarında status='running', closed_at=NULL zombie'ler   
  kalıyor.

  K2. runs:create post-insert sidecar hatası phantom row bırakıyor (sonsuza dek running)
  src-tauri/src/commands/runs.rs:119-138 — INSERT runs row → handle.start_run().await? hata fırlatırsa, fonksiyon Err  
  döner ama DB'deki status='running' row kalır, run id caller'a dönmez. Frontend bu zombie run'u sonsuza dek "running" 
  görür. Ek olarak: Python sidecar yokken try_state None döner; bu da aynı durumu yaratır (yorum bu davranışı kabul    
  ediyor ama UI ayırt edemiyor).

  K3. runs_cancel ile finalise_run arasında race — kullanıcının cancel'ı sessizce ezilebilir
  src-tauri/src/commands/runs.rs:152-173 + src-tauri/src/sidecar/agent.rs:430-449 — runs_cancel DB'yi error yapıyor ama
   sidecar'a sinyal göndermiyor. Sidecar koşmaya devam edip RunCompleted emit ediyor; finalise_run'ın UPDATE'i WHERE   
  status = 'running' koşulu YOK, dolayısıyla kullanıcının cancel'ını success ile ezebiliyor.

  K4. Sidecar shutdown() derhal start_kill() — Python event flush edemeden ölüyor
  src-tauri/src/sidecar/agent.rs:175-191 — shutdown frame yaz → stdin drop → child.start_kill()
  (TerminateProcess/SIGKILL) hiçbir grace olmadan. Read loop için JoinHandle saklanmıyor, drained edilmiyor. Pipe      
  buffer'daki run.completed frame'leri kayboluyor → DB'de running zombie run'lar.

  K5. PTY reader String::from_utf8_lossy ile chunk bazında multi-byte UTF-8 bozuyor
  src-tauri/src/sidecar/terminal.rs:677-678 — yorumda "preserves multi-byte sequences split across chunks well enough" 
  yazıyor ama bu yanlış. 3-byte char (örn. é, emoji) chunk sınırına denk geldiğinde her iki chunk'ın yarım baytları    
  U+FFFD ile değiştiriliyor. Hem live event payload'ında hem ring buffer'da bozulma. Doğru fix: Vec akümülatör + sadece
   tam satırda decode.

  K6. seed_mcp_servers başarısızlığı uygulamayı PERMANENT abort ediyor
  src-tauri/src/db.rs:73-75, 112-117 + src-tauri/src/mcp/manifests.rs:102-115 — load_all() ilk hatalı manifest'te      
  early-return; seed_mcp_servers db::init'ten çağrılıyor; setup hook hata döner, tauri::Builder::build()...expect(...) 
  panic atıyor. Tek bozuk JSON manifest = uygulama hiç açılmıyor, her relaunch tekrar crash. manifests::get() da her   
  install/call'da load_all() çağırıyor — perf değil ama aynı kırılganlık.

  K7. Commands runs_create ve mailbox emit mailbox.id/runs.id sabitliği yok
  migrations/0001_init.sql:126-132 — mailbox tablosunun PRIMARY KEY tanımı YOK, sadece rowid var. SQLite AUTOINCREMENT 
  olmadan rowid silmeden sonra reuse edebilir; commands/mailbox.rs:43 rowid'i id olarak frontend'e veriyor → React key 
  kararsızlığı, cache çakışmaları. Yorum "implicit autoincrement ordering" diyor ama bu yanlış.

  YÜKSEK (kullanıcının göreceği regresyon riski)

  Y1. runs_spans.parent_span_id REFERENCES runs_spans(id) ON DELETE clause yok (migrations/0001_init.sql:66) —
  self-referential FK + parent run'a CASCADE birleşince SQLite cascade order footgun.

  Y2. Python sidecar PYTHONUNBUFFERED set edilmiyor (src-tauri/src/sidecar/agent.rs:231-249) — Python stdout pipe'a    
  block-buffered yazar; küçük span'lar pipe buffer dolana kadar UI'a ulaşmaz.

  Y3. langchain-anthropic 0.3.x api_key= kwarg uyumsuzluğu (agent_runtime/workflows/daily_summary.py:105-109) —        
  production-only path, # pragma: no cover ile test edilmemiş; gerçek API key ile ilk run'da Pydantic ValidationError  
  ile patlıyor.

  Y4. keyring.get_password backend exception'ları NoApiKey'i bypass ediyor (agent_runtime/secrets.py:72) —
  KeyringLocked / NoKeyringError / SecretServiceUnavailable doğrudan span'a leak ediyor; "Configure API keys" UX yolu  
  ateşlenmiyor.

  Y5. Python _default_anthropic_llm non-text content block'ları sessizce drop ediyor
  (agent_runtime/workflows/daily_summary.py:111-117) — tool_use blok'lu yanıtlarda planner çıktısı boş kalıyor,        
  downstream reasoner anlamsız input'la çalışıyor.

  Y6. Zero-length frame ile clean EOF ayırt edilemez (agent_runtime/framing.py:69-72) — protocol forward-compat:       
  ileride cancel_run/heartbeat zero-body frame'leri yapılamaz.

  Y7. mailbox:list sinceTs doc'ta exclusive ama kod >= (inclusive) (commands/mailbox.rs:44, 52) — frontend cache'lenmiş
   ts'i gönderince aynı saniyedeki kayıtlar duplicate döner.

  Y8. mcp:install race-safe değil (commands/mcp.rs:49-60 + mcp/registry.rs:48-61) — concurrent install iki npx spawn,  
  iki tx, deterministic olmayan final tool listesi, çift mcp:installed event.

  Y9. tauri.conf.json: csp: null + withGlobalTauri: true (tauri.conf.json:13, 31) — production'da CSP yok + IPC handle 
  global; defense-in-depth açığı, prod release blocker.

  Y10. bindings.ts typedError Error instance'ında rethrow ediyor (app/src/lib/bindings.ts:418-425) — typed
  {status:"error", error} branch'ı pratikte hiçbir zaman dönmüyor; WP-W2-08 frontend kontratını yanlış varsayacak.     

  Y11. verbatimModuleSyntax: true + bindings.ts type re-export'ları (app/tsconfig.app.json:13) — WP-W2-08'de import {  
  Agent } from '../lib/bindings' TS1484 ile patlayacak; her tüketici import type zorunlu.

  Y12. vite.config.ts: sourcemap: true prod build'e source map shipt ediyor — desktop binary unpacked source'u expose  
  ediyor.

  Y13. ESLint react/react-hooks plugin'i yok (app/eslint.config.js:8) — WP-W2-08 hook'larında Rules of Hooks ihlalleri 
  lint'ten geçecek.

  Y14. .bridgespace/ gitignore'da yok — agent git add -A yaparsa accidental commit; AGENT_LOG zaten flag'lemiş ama     
  follow-up landlemedi.

  Y15. terminal:write/spawn data ve cmd üst sınırı yok (commands/terminal.rs:65-74, 44-58) — kullanıcı kontrollü cwd   
  path traversal + arbitrary cmd execution. Bugünkü trust boundary "yerel frontend" olduğu için güvenli ama
  defense-in-depth açığı.

  Y16. runs.status CHECK ('running','success','error') — cancelled yok (migrations/0001_init.sql:57) — kullanıcı       
  cancel'ı error ile karışıyor.

  Y17. App.tsx'te JSX.Element annotation'ı React 19'da kırılacak — kapsam dışı bir bump bile bütün annotation'ları     
  patlatır.

  Y18. panes.status CHECK constraint yok ama doc'lanmış 6 değer var + kill_pane yorumda olmayan 'closed' değerini      
  yazıyor (terminal.rs:514) — frontend pattern-matching closed'ı bilmiyor.

  ORTA

  - runs_list / mailbox_list pagination yok — büyük history'de unbounded IPC payload.
  - flush_ring_to_db ve panes.status UPDATE'i ayrı tx — SIGKILL arasında inconsistent state.
  - mcp_uninstall running session'ı kapatmıyor (Week 2 etkisiz, Week 3 pool eklenince problem).
  - agents:update read-then-write transactionsız — concurrent delete ile race.
  - infer_agent_kind substring match not-claude-code-fake.sh gibi false-positive.
  - expand_cwd %USERPROFILE% Windows env-var syntax'ını expand etmiyor.
  - tokenize_command Windows path with spaces / unterminated quotes yumuşak handle ediyor.
  - read_loop pending: String hattında newline yoksa unbounded büyür (DoS shape).
  - set_status task spawn'ları sıralanmamış → terminal status regression (running → awaiting_approval → success        
  arasında race).
  - run_waiter try_wait hatası senthetik exit_code(1) yazıp child'ı orphan bırakıyor.
  - Python to_wire() non-JSON-serializable attrs'ye crash; span permanent is_running=true.
  - _now_ms ms granülarite — back-to-back spans t0_ms aynı; UX timeline ambigüite.
  - Python read_in_executor shutdown'da cancel edilemiyor.
  - Mock tool çıktıları MOCK: marker'ı yok — real veriyle karışır.
  - langchain-core pyproject.toml'da pin'li değil — minor drift riski.
  - bundle.publisher/copyright/shortDescription yok — Windows SmartScreen "Unknown publisher" + macOS code-signing.    
  - External Google Fonts @import (Neuron Design/colors_and_type.css:7) — CSP eklendiğinde block + cold-start latency. 
  - Bundle id docs drift: WP-W2-02-sqlite-schema.md hâlâ com.neuron.dev yazıyor (gerçek: app.neuron.desktop).
  - health_db error type string, diğer komutlar AppErrorWire — frontend error helper asymmetry.
  - vite.config.ts host:"127.0.0.1" IPv6-preferred host'larda Tauri's localhost ile mismatch.
  - App.test.tsx OKLCH assertion'ı CSS-load race'e açık (jsdom timing'de flaky olabilir).
  - Tauri capability core:default core:event:emit veriyor → frontend script mailbox:new event'lerini spoof edebilir.   
  - agents.temp 0..2 range CHECK yok (kod side validate ediyor ama DB seviyesinde yok).
  - bindings.ts mcpCallTool docstring'inde "cermony" typo'su (kaynak: commands/mcp.rs).

  DÜŞÜK

  - /src-tauri/src/error.rs — McpProtocol ve McpServerSpawnFailed kind doc table'ında listelenmemiş.
  - runs_cancel mesajı: "Run X is success, not running" — awkward gramer.
  - now_seconds() clock skew'da 0 döner; DB'de epoch 1970 inserts.
  - kill_pane "already dead" detection string-match — os error 87 non-İngilizce locale'de muhtemelen güvenli ama       
  kırılgan.
  - WireSpan manuel re-serialize (11 alan) — derive(Serialize) direkt kullanılmamış, drift riski.
  - framing.rs doc'ta TooLarge "recoverable" diyor ama desync recovery yok — kod zaten fail-stop, doc yanıltıcı.       
  - seed_demo_workflow workflows.name UNIQUE çakışmasında INSERT OR IGNORE sessizce no-op — silent inconsistency.      
  - models::Pane.cwd: String to_string_lossy() ile lossy roundtrip (rare non-UTF-8 paths).
  - pyproject.toml'da requires-python = ">=3.11,<3.13" ama .python-version: 3.11 — dual source of truth.
  - Python pytest-asyncio asyncio_mode="auto" + redundant @pytest.mark.asyncio decorators.
  - MailboxEntry.entry_type rename ile type JSON anahtarı — verified match (bug değil, doğrulama notu).

  Yanlış-Pozitif (subagent flag'ledi, doğruladım — gerçek değil)

  - ❌ tauri::async_runtime::block_on deadlock (Rust core C1) — default multi-thread runtime'da güvenli, sadece        
  kullanıcı set() ile single-thread yaparsa.
  - ❌ tauri-specta builder iki kez consume (Rust core C4) — invoke_handler() ve mount_events() ikisi de &self (kanıt: 
  tauri-specta-2.0.0-rc.24/src/builder.rs:310,336); proje derleniyor + testler geçiyor.
  - ❌ ContentBlock #[serde(other)] internally-tagged compile fail (Rust core H12) — serde unit variant + internally   
  tagged için bunu destekler; testler geçiyor.
  - ❌ MCP client::request timeout desync (subagent commands H11 / aynı temada Rust core H11) — request &mut self      
  alıyor + registry her çağrıda respawn yapıyor; client drop'ta kill_on_drop var. Mevcut Week 2 yapısında riskli değil.

  Tavsiye Edilen Öncelik Sırası (top 7)

  1. K1 shutdown_all'da her pane için sync flush + panes.status='closed' UPDATE.
  2. K6 + C3 manifest yükleme: ya bireysel manifest hatası tek server'ı stub'a düşürsün ya da seed başarısızlığı app   
  crash etmesin.
  3. K3 finalise_run UPDATE'ine WHERE status = 'running' guard.
  4. K4 sidecar shutdown: read-loop JoinHandle sakla, 2-3s grace ile drain et, sonra kill.
  5. K5 PTY reader Vec accumulator + UTF-8 sınır algılama.
  6. K2 runs:create sidecar fail'inde row'u atomic delete et veya status='error' set et.
  7. K7 mailbox tablosuna id INTEGER PRIMARY KEY AUTOINCREMENT ekle (migrate gerekecek).

  Özet

  ┌────────────────┬──────┐
  │    Kategori    │ Sayı │
  ├────────────────┼──────┤
  │ Kritik         │ 7    │
  ├────────────────┼──────┤
  │ Yüksek         │ 18   │
  ├────────────────┼──────┤
  │ Orta           │ 25   │
  ├────────────────┼──────┤
  │ Düşük          │ 11   │
  └────────────────┴──────┘

  Genel durum: Mevcut Week 2 acceptance gate'leri (cargo test/pytest/vitest hepsi yeşil) bug yokluğunu kanıtlamıyor —  
  testler happy-path'i kapsıyor, shutdown/race/error-path coverage'ı zayıf. Kritiklerin tamamı shutdown disipliniyle   
  ilgili: app exit yolunda iki sidecar (Python ve PTY) kayıp veriye yol açıyor, runs:create rollback yok, manifest tek 
  hatası app'i bricklemiyor. WP-W2-08 (frontend wiring)'e geçmeden önce K1-K7 düzeltilmeli; bunlar düzeltilmediğinde   
  ileride çok daha yüksek maliyetli regresyonlar oluşturacak.

● Agent "Audit MCP module for bugs" completed

● MCP Audit Geç Geldi — Eklenecek Bulgular
                                                              
  MCP-spesifik subagent rapor verirken benim raporum yayınlanmıştı. Yeni kritik bulgular ekleniyor:
                                                                                                                         Yeni KRİTİK
                                                                                                                       
  K8 (MCP). read_line boş satırları Ok(Some("")) döndürüyor, request() bunu serde_json::from_str("")'e besleyip "decode
   response: EOF" ile crash oluyor                                                                                       src-tauri/src/mcp/client.rs:399-404 → :255-268. Yorumda "Some servers emit blank keepalive lines; ignore" yazıyor ama   kod yalnızca boş string döndürüyor; çağıran taraf bunu skip etmiyor. npx/Node bazen başlangıçta \n\n stray emit eder   → install handshake'i kırar.                                                                                                                                                                                                               
  K9 (MCP). CallToolOutput.content için #[serde(default)] yok — geçerli {isError:false} cevapları decode hatasıyla
  reddediliyor
  src-tauri/src/mcp/client.rs:67-73 → :329-331. MCP spec'e göre content opsiyoneldir. write_file benzeri side-effect   
  tool'larında server {} veya {"content":[]} döndürebilir; kod sadece dolu array kabul ediyor.

  Yeni YÜKSEK

  Y19 (MCP). Response.id: Option<u64> ile id: null JSON-RPC error response'ları yanlış reject ediliyor
  client.rs:111-128 → :277-285. JSON-RPC 2.0 §5.1: server request id'yi belirleyemiyorsa id: null döner. Kod bunu "id  
  mismatch" hatasına dönüştürüyor; gerçek error message kayboluyor.

  Y20 (MCP). tokio::time::timeout(read_line) cancellation-safe değil — Week 3 pooling'de stream corruption
  client.rs:247-254. Tokio dokümantasyonu açıkça uyarıyor: read_line cancellation-unsafe; timeout fire ettiğinde       
  BufReader'ın iç buffer'ında kısmi bytes kalır. Bugün her çağrıda client respawn olduğu için riskli değil, ama yorumda
   "Week-3'te long-lived connection" niyeti var; pool'a geçince corrupted reader.

  Y21 (MCP). argsJson: String üst sınır yok — IPC payload DoS
  commands/mcp.rs:107-113. Frontend kontrolünde olsa da defense-in-depth açığı; arbitrary blob serde_json::from_str ve 
  sonra child stdin write'a gidiyor.

  Y22 (MCP). initialize response'undaki protocolVersion doğrulanmıyor
  client.rs:215 — let _ = self.request(...) sonucu discard ediliyor. MCP spec SHOULD verify olarak isteyişiyle uyumsuz;
   farklı protokol sürümlü server sessizce başarısız tools/list'e gider.

  Yeni ORTA

  - MCP Command::env'i parent env temizlemiyor (client.rs:160-171) — npx child tüm parent env vars'ı miras alıyor      
  (AWS_*, OPENAI_API_KEY, vb.); third-party MCP server'a istemeden secret leak.
  - MCP tools/list sonucu {"tools": null} decode ederse panic'e benzer hata, sadece missing field handle ediliyor      
  (client.rs:308-319).
  - MCP serde error mesajları offending line'ı UI'a basıyor (client.rs:262-267) — auth payload'ı varsa info leak.      
  - MCP shutdown'da stdout drain edilmiyor — child stdin EOF aldıktan sonra büyük response yazıyorsa pipe dolar, 1s    
  grace boşa harcanır.
  - MCP mcp:installed emit DB commit sonrası ama emit fail durumunda DB'de installed=1, frontend'e Err
  (commands/mcp.rs:54-59) — "successful but errored" UX.

  Güncellenmiş Toplam

  ┌──────────┬────────┬────────────────┬────────┐
  │ Kategori │ Önceki │     + MCP      │ Toplam │
  ├──────────┼────────┼────────────────┼────────┤
  │ Kritik   │ 7      │ +2             │ 9      │
  ├──────────┼────────┼────────────────┼────────┤
  │ Yüksek   │ 18     │ +4             │ 22     │
  ├──────────┼────────┼────────────────┼────────┤
  ├──────────┼────────┼────────────────┼────────┤
  │ Orta     │ 25     │ +5             │ 30     │
  ├──────────┼────────┼────────────────┼────────┤
  │ Düşük    │ 11     │ (~14 ek küçük) │ ~25    │
  └──────────┴────────┴────────────────┴────────┘

  Top Önceliklere Eklenenler

  8. K8 (MCP read_line blank line) — install handshake'lerinde sporadik crash; cargo testlerinde mock duplex stream    
  kullanıldığı için yakalanmıyor.
  9. K9 (CallToolOutput.content default) — gelecek tool çağrılarını sessizce kıracak; tek satır fix
  (#[serde(default)]).
  10. Y19 (id: null mishandling) — debugging deneyimini kuran error message'ı yanlış string'e çeviriyor.

  Kapsam itirafı: MCP integration test'i #[ignore] olduğundan CI bu yolun hiçbirini koşmuyor. Mock duplex test'leri    
  sadece happy-path NDJSON round-trip'i kapsıyor; blank line, missing content, null-id error, concurrent
  install/uninstall hiçbir test'te yok. Mevcut "75 passed, 2 ignored" rakamı kapsama gerçeğini gizliyor.

  Tüm rapor (önceki sentez + bu MCP eki) Neuron projesinin Week 2 tamamlanmadan önce dikkat edilmesi gereken 9 kritik +
   22 yüksek bulguyu içeriyor; çoğu shutdown disiplini, error path coverage ve manifest yükleme tek-noktasıyla ilgili.