# Neuron — Otonom İyileştirme Backlog'u

**Tohumlama:** 2026-05-31 (ilk inceleme). Her tur bu dosyayı okur, çözdüğünü işaretler, yeni doğanı ekler.
**Statü kodları:** ☐ açık · ◐ kısmen · ☑ çözüldü · ⏸ bloklu (büyük/parçalı PR) · 🔥 hot-zone (audit-only, W6 aktif)

> Öncelik: kırık tab / kırmızı kapı > Tier 1 > Tier 2 > Tier 3. Mod: stable→apply, dirty→audit. Her madde: **etki · risk · kanıt**.

> **2026-06-10 — ultracode tam-proje denetimi (Claude, kullanıcı talebiyle):** 9-boyutlu çok-ajanlı tarama (91 bulgu, 78 adversarial-doğrulanmış) + uygulama. Çalışma ağacı **tamamen temiz/commit'li** → hot-zone kısıtı kalktı. Kapananlar: **T1-01 ☑** (Terminal + TerminalSwarm render-smoke testleri eklendi — tüm 9 tab kapsandı), **T1-04 ☑** (TerminalSwarm isError/isLoading; RunInspector boş-pane; Settings Keys probe-error), **T1-05 ◐→büyük ölçüde ☑** (swarm-term.css tanımsız token sözlüğü ~40 site + faz paleti 16 hex → --syn-* türevi --phase-*; routing-log oklch literalleri → token; kalan: canvas.css dup token-sheet konsolidasyonu — bilinçli ertelendi, canlı değer drift'i kazanan-seçme kararı istiyor), **T1-06 ☑** (ölü CSS: .sb-section/.sb-leaf/.topbar-sub/.ins-pulse/.tl-*/.term-cursor*/.swarm-goal-*/.swarm-term-picker + ProjectPicker.tsx/useWorkflows.ts/useAgentUpdate/appErrorCopy + Rust: pane_chunk/registry_from/Topology/JobDetail::from_job/seed_server_uninstalled), **T1-07 ☑** (kalan a11y: SwarmJobList/NodeCard/SpanRow/pane-tab-close klavye + aria-label'lar + ⌘K gerçek kısayol), **T2-01 ◐** (PaneBody≅SwarmPane ~130L dup → paylaşılan `useXtermPane` hook'u; Terminal.tsx 654→~480L; tam bileşen-dosya split'i açık), **T2-02/T2-03 ☑** (useTauriEvent migrasyonu 8 kopyada tamamlandı), **T3-05 ☑** (üretimde println/eprintln yok — tarayıcı doğruladı). **Yeni split'ler: T3-19/20/21 ☑** — `commands/mailbox/`, `commands/runs/`, `db/` (T3-01 deseni; MIGRATOR manifest-dir-göreli olduğundan etkilenmedi). **13 backend bug düzeltildi** (`07ffd7e`): 3× UTF-8 char-boundary panic (16KiB scan cap; ortak `crate::text::truncate_to_char_boundary`), strip_csi ESC+multibyte panic, 2× notify_waiters kayıp-uyandırma (projector shutdown / bridge uninstall → notify_one), TerminalSwarmRegistry::start TOCTOU (StartReservation guard), swarm_term_run_update in-flight+timeout guard'ı, brain AgentResult deadline kolu (LoopEventOutcome::TimedOut), mcp uninstall tx, pane_lines UNIQUE+OR IGNORE (migration **0012**), reader force-flush UTF-8 sınırı, agent slot Spawning-stuck, persona tmp Drop-guard, dummy_child_stdin kaldırıldı (stdin: Option). **ProjectPickerRoute nihayet bağlandı** (`744dbec`) — proje gate'i + topbar chip; App.test buna göre güncellendi. **Gate'ler:** cargo test 532 ✓ / vitest **102** ✓ / tsc ✓ / eslint ✓. **Bilinçli ertelenenler:** canvas.css token-sheet konsolidasyonu; useRoutingEvents modül-store yeniden yapılandırması (hook component-lifetime semantiğini belgeliyor — tasarım kararı); tier_of/state_of silimi (kendi testleri var, swarm_term UI işi tüketebilir); swarm:: DRY çıkarımları (llm_json.rs ×5 kopya, spawn-block dup, truncate_chars ×9 — swarm:: refactor-freeze sürüyor; bug-fix'ler freeze'den muaf tutuldu); emit_internal (ölü ama pub API, doğrulayıcı onayı yok). Kalan stable büyük tek-dosya: `lib.rs` (~502L, setup/bootstrap — riskli, en sona).

> **2026-06-08 — manuel oturum (Claude, kullanıcı talebiyle):** W6 swarm_term işi `7b77e8b`'de **commit'lendi** → eski hot-zone'un büyük kısmı (`App.tsx`, `swarm-term.css`, `useRoutingEvents`, 9 persona, `swarm_term/*.rs`) artık **stable**. Yalnız 5 frontend dosyası kullanıcı WIP'i olarak dirty kaldı: `SwarmPane.tsx`, `usePaneLines.ts`, `bindings.ts`, `Terminal.tsx`, `TerminalSwarm.tsx` (bunlara dokunulmadı). Bu turda elle kapatıldı: **T1-02 / T1-03 / T1-07 / T2-02 ☑**, **T1-01 / T1-04 ◐ ilerledi**. İki pre-existing kırık giderildi: Rust `sidecar/terminal/reader.rs:29 display_safe_len` hayalet import + vitest'teki 2 bayat orchestrator-decide testi (PascalCase `kind` + AppErrorClient lokalizasyonu öncesi ham-mesaj beklentisi). **Tüm gate'ler yeşil:** cargo check ✓ / vitest 83-test ✓ / tsc ✓ / eslint ✓. Tier-3 backend split'leri `refactor/tier3-splits` dalına commit'lendi. Sonraki tur artık stable frontend'i (Terminal-swarm hariç) apply edebilir.

---

## Tier 1 — Tab fonksiyonel sağlık + UI/UX (kullanıcının açık önceliği)

### T1-01 ◐ 9-tab render smoke testi tamamla
- **Etki:** "her tab çalışıyor mu" garantisini teste bağlar (kullanıcının birincil isteği).
- **Tab envanteri** (`app/src/App.tsx` NAV): `canvas`(Workflow), `terminal`, `terminal-swarm`, `swarm`, `agents`, `runs`, `mcp`, `routing-log`, `settings` + `RunInspector` (canvas yan panel).
- **Mevcut kapsam (2026-05-31_1400 turunda haritalandı):**
  - `App.test.tsx` (hot-zone) shell üzerinden tıklayarak: canvas, terminal, agents, runs, mcp, RunInspector — yalnız populated render (loading/empty yok).
  - `SwarmRoute.test.tsx` (stable): swarm — bağımsız.
  - **YENİ bu tur (apply):** `AgentsRoute.test.tsx` + `MCPRoute.test.tsx` (stable) — bağımsız, izole; loading + populated + empty-state üçlüsü (App.test.tsx'in atladığı durumlar).
- **Kalan boşluk → hepsi hot-zone (dirty W6) → AUDIT-only, W6 commit'lenince apply:** `terminal-swarm` (TerminalSwarm.tsx), `routing-log` (RoutingLogRoute.tsx), `settings` (SettingsRoute.tsx) — hiçbirinin render-smoke'u yok.
- **Risk:** düşük (test ekleme). **Mod:** apply (stable route'lar) / audit (dirty route'lar).

### T1-02 ☑ ErrorBoundary'ye düşen route taraması
- **Etki:** bir tab açılışta patlıyorsa kullanıcı "Couldn't load …" kartı görür; bunları sıfıra indir.
- **Yapılacak:** her route'u mount edip ErrorBoundary fallback'ine düşeni tespit eden test; düşen varsa kök neden + düzeltme.
- **Risk:** orta (gerçek bug çıkabilir). **Mod:** apply (stable route) / audit (TerminalSwarm dirty ise).

### T1-03 ☑ Stale App.tsx başlık yorumu + "RouteStub coming soon"
- **Kanıt:** `app/src/App.tsx:1-5` "Routes are stubs (coming soon)" diyor ama route'lar gerçek bileşenlere bağlı. `RouteStub`/`route-stub` default-case ölü olabilir.
- **Yapılacak:** yorumu güncelle; `RouteStub` hâlâ erişilebilir mi kontrol et, değilse sil.
- **Risk:** düşük. **Mod:** apply.

### T1-04 ◐ Loading / empty / error durum denetimi (UX)
- **Etki:** TanStack Query hook'ları olan her route'ta `isLoading`/`isError`/boş-veri durumları tutarlı mı? Eksikler kullanıcıya boş/çakık ekran gösterir.
- **Yapılacak:** route bazında denetle; eksik state'leri tutarlı bir desene (skeleton/empty card) bağla.
- **Risk:** düşük-orta. **Mod:** apply.

### T1-05 ☐ OKLCH + design-system parity denetimi
- **Etki:** Charter #4 (OKLCH only). Yeni CSS'te hex/HSL kaçağı = ihlal.
- **Yapılacak:** `app/src/styles/*.css` (`app.css`, `swarm-term.css`, `terminal.css`) içinde hex/HSL tara; `design-system-spec.md`'ye göre token sapmalarını düzelt.
- **Risk:** düşük. **Mod:** apply (SVG içindeki legacy hex hariç — Charter izinli).

### T1-06 ☐ Ölü CSS / kullanılmayan bileşen
- **Kanıt:** `RoutingOverlay.tsx` silinmiş (git status) — ona ait CSS/import kaldı mı? Yeni `ToastHost`, `SwarmHierarchy` eklenmiş — stil eşleşmesi tam mı?
- **Yapılacak:** kullanılmayan CSS sınıfı / export / import tara ve sil.
- **Risk:** düşük. **Mod:** apply.

### T1-07 ☑ Erişilebilirlik (a11y) temel geçiş
- **Etki:** UX kolaylığı. Sidebar `li` tıklanabilir ama `role`/klavye yok (`App.tsx:93-103`); arama input'u `⌘K` kbd var ama bağlı kısayol var mı?
- **Yapılacak:** tıklanabilir `li`/`div`'lere rol+klavye, ikon-only butonlara `aria-label`.
- **Risk:** düşük. **Mod:** apply.

---

## Tier 2 — Frontend refactor (stable zone)

### T2-01 ☐ `app/src/routes/Terminal.tsx` (654L) bölünmesi
- **Etki:** en büyük frontend route; xterm kurulum + pane state + render bir arada.
- **Yapılacak:** xterm/PTY köprü mantığını hook'a (`useTerminalPane`), sunumu alt-bileşene ayır. Davranış birebir korunur.
- **Risk:** orta (xterm yan etkileri). **Mod:** apply — **ama** snapshot'ta dirty ise (W6) → 🔥 audit.

### T2-02 ☑ `app/src/components/OrchestratorChatPanel.tsx` (322L)
- **Etki:** test'i (359L) kendisinden büyük → karmaşık. Sunum/efekt ayrımı.
- **Risk:** orta. **Mod:** apply.

### T2-03 ☐ Hook DRY denetimi (`app/src/hooks/`)
- **Kanıt:** çok sayıda yeni hook (`useRoutingEvents` 302L, `useAppearance`, `useActiveProject`, `useClaudeUpdateProgress`, `useTerminalSwarmSession`). Tekrarlı invoke/listen/cleanup desenleri merkezleşebilir.
- **Risk:** düşük-orta. **Mod:** apply (dirty olanlar audit).

---

## Tier 3 — Backend refactor (stable zone) + teknik borç

### T3-01 ☑ `src-tauri/src/commands/swarm.rs` (2878L) — namespace + test modülü bölündü
- **Etki:** en büyük Rust dosyasıydı; komut namespace'i + ~1824L test bloku tek dosyadaydı.
- **Yapıldı (komutlar, önceki tur):** 2878→1908L — `commands/swarm/{agents,dispatch,jobs,orchestrator,profiles,run}.rs`.
- **Yapıldı (test modülü, 2026-06-03_1400 DEEP):** kalan 1908L'nin ~1824L'si `#[cfg(test)] mod tests` blokuydu → `commands/swarm/tests.rs`'e taşındı (4-boşluk dedent). Kök artık **82L** (docs + `pub mod`/`pub use` + `workspace_agents_dir` + `mod tests;`). `super` yolu değişmedi → `use super::*;` aynı; public yüzey birebir. **Kanıt:** `cargo check --all-targets` (pre-existing W6 `reader.rs` kırığı geçici bypass'lı) → `Finished`, `commands/swarm` 0 tanı.

### T3-02 ◐ `swarm/projector.rs` (2430L) · `brain.rs` (2013L) · `agent_dispatcher.rs` (2118L)
- **Etki:** swarm çekirdeği; her biri tek dosyada çok sorumluluk.
- **Durum:** `projector.rs` (önceki turlar) ve `brain.rs` (brain/ paketi) bölündü. **`agent_dispatcher.rs` (2118L) → `agent_dispatcher/` 6-dosya paketi (2026-06-02_0953, DEEP)** — bkz. Çözülenler. Kalan: yok (bu grup kapandı; yeni dev dosya doğarsa ayrı madde aç).
- **Risk:** yüksek. **Mod:** ⏸ audit + parçalı plan; küçük, izole saf-fonksiyon çıkarımları apply edilebilir.

### T3-06 ☑ `swarm/coordinator/job.rs` (1471L) — modül paketine bölündü
- **Etki:** en büyük stable backend dosyalarından biri; job domain-model tipleri + durumlu `JobRegistry` (kilit/cancel/SQL write-through) + `SwarmJobEvent` + ~520L test tek dosyadaydı.
- **Yapıldı (2026-06-02_2100, DEEP):** `coordinator/job.rs` → `coordinator/job/` 6-dosya paketi:
  `mod.rs` (modül docs + re-export), `state.rs` (`JobState` enum + db-str), `model.rs` (`StageResult`/`Job`/`JobOutcome`/`JobSummary`/`JobDetail`), `registry.rs` (`JobRegistry` + impl + `Default`), `event.rs` (`SwarmJobEvent`), `tests.rs`. Public yüzey aynen: `coordinator::job::{Job, JobDetail, JobOutcome, JobRegistry, JobState, JobSummary, StageResult, SwarmJobEvent}` re-export'ları korundu → `coordinator/mod.rs` `pub use job::{…}`, `store.rs` `use super::job::{…}`, `lib.rs`/`projector/*` tüketicileri değişmedi.
- **Görünürlük:** `store::*` fn'leri `pub(super)` (super=coordinator); `registry.rs` coordinator torunu olduğu için erişebiliyor. Davranış birebir. **Kanıt:** `cargo check --all-targets` → coordinator/job'dan **0 hata/uyarı** (yalnız pre-existing hot-zone W6 kırığı `sidecar/terminal/reader.rs:29 display_safe_len`, bu refactor'la ilgisiz).

### T3-03 ☑ `src-tauri/src/sidecar/terminal.rs` (2187L) — modül paketine bölündü
- **Etki:** PTY supervisor + ring buffer + ANSI strip + reader/waiter bir aradaydı.
- **Yapıldı (2026-06-01_1400, DEEP):** `terminal.rs` → `terminal/` paketi:
  `mod.rs` (çekirdek `TerminalRegistry` + `PaneState`/`RingLine`/`RegistryInner` + `status` + public yüzey),
  `text.rs` (saf `strip_csi`/`trim_terminal_line_end`/`extract_dsr_cpr_queries`),
  `command.rs` (`default_shell`/`tokenize_command`/`expand_cwd`/`infer_agent_kind` + claude env-scrub listesi),
  `approval.rs` (per-agent regex setleri + `extract_approval_blob`),
  `reader.rs` (`run_reader`/`run_waiter`/emit/status-geçiş/`flush_ring_to_db`),
  `tests.rs` (tüm test modülü). Public yüzey aynen: `TerminalRegistry`, `registry_from`.
- **Görünürlük:** alt-modül fn'leri `pub(super)`; struct alanları private kalır (descendant erişim). Davranış birebir.
- **Kanıt:** `cargo check --all-targets` yeşil; `terminal/*`'tan sıfır uyarı.

### T3-07 ☑ `swarm/mailbox_bus.rs` (1458L) — modül paketine bölündü
- **Etki:** en büyük stable backend dosyasıydı; `MailboxEvent` tagged-enum + `MailboxEnvelope` wire-shape + durumlu `MailboxBus` (per-workspace broadcast + persist/emit + workspace-busy guard + shutdown cancel) + ~820L test tek dosyadaydı.
- **Yapıldı (2026-06-03_2100, DEEP):** `mailbox_bus.rs` → `mailbox_bus/` 5-dosya paketi: `mod.rs` (modül docs + re-export), `event.rs` (`MailboxEvent` + `kind_str`/`from_row_parts`), `envelope.rs` (`MailboxEnvelope`), `bus.rs` (`MailboxBus` + sabitler `BROADCAST_CAPACITY`/`LIST_TYPED_*`), `tests.rs` (tüm test modülü). Public yüzey aynen: `swarm::mailbox_bus::{MailboxBus, MailboxEnvelope, MailboxEvent}` → `swarm/mod.rs:73` `pub use` ve tüm tüketiciler (projector/brain/agent_dispatcher/commands/agent_registry/lib.rs) değişmedi. Davranış birebir. **Kanıt:** `cargo check --all-targets` (pre-existing W6 `reader.rs:29 display_safe_len` kırığı geçici bypass'lı) → `Finished` exit 0, mailbox_bus'tan 0 tanı; reader.rs `git checkout` ile birebir geri yüklendi.

### T3-08 ☑ `swarm/coordinator/store.rs` (1292L) — modül paketine bölündü
- **Etki:** en büyük kalan **stable** backend dosyasıydı; tüm SQLite write-through/read helper'ları + JSON kolon kodek'leri + goal-truncation + ~747L test tek dosyadaydı.
- **Yapıldı (2026-06-04_1457, DEEP):** `coordinator/store.rs` → `coordinator/store/` 5-dosya paketi: `mod.rs` (büyük modül docs bloğu birebir + re-export), `cols.rs` (`serialize/deserialize_verdict`, `serialize/deserialize_decision`, `truncate_chars`), `write.rs` (`insert_job_and_lock`/`update_job`/`insert_stage`/`delete_workspace_lock`), `read.rs` (`list_jobs`/`get_job_detail`/`fetch_stages`(priv)/`list_recent_jobs_full`/`detail_to_job`), `recovery.rs` (`recover_orphans` + `RecoveredOrphans`), `tests.rs` (tüm test modülü, 4-boşluk dedent).
- **Görünürlük:** orijinal `pub(super)` (super=coordinator) yüzeyi **birebir** korundu — coordinator'a açılan item'lar `pub(in crate::swarm::coordinator)`, mod.rs `pub(super) use {…}` ile flat `store::fn` yolunu yeniden yayımlıyor. Submodül-içi paylaşılan leaf helper'lar (`cols::*`, `read::detail_to_job`) `pub(super)` (store-scope). Tek tüketici `coordinator/job/registry.rs:31 use crate::swarm::coordinator::store;` + `store::*` çağrıları **değişmedi**. Davranış birebir.
- **Kanıt:** `cargo check --all-targets` (pre-existing W6 `reader.rs:29 display_safe_len` kırığı geçici bypass'lı) → `Finished` exit 0, `coordinator/store`'dan 0 hata/uyarı; reader.rs `git checkout` ile birebir geri yüklendi.

### T3-09 ☑ `swarm/agent_registry.rs` (1029L) — modül paketine bölündü
- **Etki:** en büyük kalan **stable** backend dosyasıydı; W4-02 `SwarmAgentRegistry` (RwLock session map + per-agent slot Mutex + W5-02 dispatcher map) + `AgentStatus`/`AgentStatusRow` wire + W4-03 `SwarmAgentEvent`/`agent_event_channel` + turn-cap env çözümü + ~350L test tek dosyadaydı.
- **Yapıldı (2026-06-05_1400, DEEP):** `agent_registry.rs` → `agent_registry/` 7-dosya paketi: `mod.rs` (modül docs birebir + layout notu + re-export), `status.rs` (`AgentStatus` enum + `AgentStatusRow`), `event.rs` (`SwarmAgentEvent` + `agent_event_channel`), `config.rs` (`DEFAULT_TURN_CAP`/`TURN_CAP_ENV` + `resolve_turn_cap`), `slot.rs` (internal `AgentSlot` + `new`), `registry.rs` (`SwarmAgentRegistry` struct + tüm impl: `new`/`with_turn_cap`/`acquire_and_invoke_turn`/`list_status`/`shutdown_workspace`/`shutdown_all`/`ensure_dispatcher`/diagnostik), `tests.rs` (tüm test modülü).
- **Görünürlük:** public yüzey **birebir** — `mod.rs` `pub use {event::{agent_event_channel, SwarmAgentEvent}, registry::SwarmAgentRegistry, status::{AgentStatus, AgentStatusRow}}` ile `swarm::agent_registry::{…}` yolunu yeniden yayımlıyor; `swarm/mod.rs:65-68 pub use agent_registry::{…}` ve tüm tüketiciler (lib.rs, commands/swarm/{agents,dispatch,run,tests}, brain/invoker, agent_dispatcher/invoker) **değişmedi**. Submodül-içi item'lar `pub(super)`: `config::*` (turn-cap), `slot::AgentSlot` + alanları (registry alan erişimi için). Davranış birebir.
- **Kanıt:** `cargo check --all-targets` (pre-existing W6 `reader.rs:29 display_safe_len` kırığı geçici bypass'lı) → `Finished` exit 0, `agent_registry`'den 0 hata/uyarı; text.rs `git checkout` ile birebir geri yüklendi.

### T3-10 ☑ `swarm/profile.rs` (1007L) — modül paketine bölündü
- **Etki:** en büyük kalan **stable** backend dosyasıydı; `.md` frontmatter loader — `PermissionMode`/`Profile`/`ProfileSource` wire-tipleri + durumlu `ProfileRegistry` (bundled `include_dir!` + workspace override yükleyiciler `load_from`/`load_term`) + elle yazılmış frontmatter parser (`parse_profile`/`required`/`parse_allowed_tools` + `id_regex`/`version_regex`) + ~363L test tek dosyadaydı.
- **Yapıldı (2026-06-05_2100, DEEP):** `profile.rs` → `profile/` 5-dosya paketi: `mod.rs` (modül docs birebir + layout notu + re-export), `types.rs` (`PermissionMode` enum + `pub(super) parse` + `Profile` struct + `ProfileSource` enum/impl), `registry.rs` (`BUNDLED_AGENTS` static + `ProfileRegistry` struct + tüm impl: `load_from`/`get`/`source`/`list`/`profile_count`/`load_term`), `parser.rs` (`id_regex`/`version_regex` priv + `pub(super) parse_profile` + `required` priv + `pub(super) parse_allowed_tools`), `tests.rs` (tüm test modülü).
- **Görünürlük:** public yüzey **birebir** — `mod.rs` `pub use {registry::ProfileRegistry, types::{PermissionMode, Profile, ProfileSource}}` ile `swarm::profile::{…}` yolunu yeniden yayımlıyor; `swarm/mod.rs:75-77 pub use profile::{…}` ve tüm tüketiciler (models.rs, binding.rs, agent_registry/{registry,tests}, persistent_session.rs, transport.rs, swarm_term/session.rs) **değişmedi**. Submodül-paylaşımlı item'lar `pub(super)`: `PermissionMode::parse`, `parse_profile`, `parse_allowed_tools`. `tests.rs` açık `use super::{parser::…, registry::…, types::…}` import'larıyla çözülüyor. Davranış birebir.
- **Kanıt:** bypass'lı `cargo check --all-targets` → `Finished` exit 0, `profile`'dan 0 tanı; bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (kullanılmayan import, bu refactor'la ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.

### T3-11 ☑ `swarm/persistent_session.rs` (991L) — modül paketine bölündü
- **Etki:** en büyük kalan **stable** backend dosyasıydı; WP-W4-01 `PersistentSession` çok-turlu `claude` subprocess'i — `TurnStreamEvent` hot-path enum + durumlu session (spawn/invoke_turn/turns_taken/profile_id/shutdown/Drop + `dummy_child_stdin`) + stdout read-loop (`read_until_result`/`drain_post_cancel`/`InvokeAccum` + post-cancel drain sabitleri) + `attach_stderr_tail` + ~412L test tek dosyadaydı.
- **Yapıldı (2026-06-06_1400, DEEP):** `persistent_session.rs` → `persistent_session/` 4-üretim+1-test dosya paketi: `mod.rs` (modül docs birebir + layout notu + re-export), `event.rs` (`TurnStreamEvent` enum + dep-asiklik notu), `read.rs` (`POST_CANCEL_DRAIN_*` sabitleri priv + `pub(super) read_until_result` + `pub(super) drain_post_cancel` + `pub(super) InvokeAccum`), `session.rs` (`SHUTDOWN_GRACE_BUDGET` priv + `PersistentSession` struct/impl + `Drop` + `dummy_child_stdin` priv + `attach_stderr_tail` priv), `tests.rs` (tüm test modülü, 4-boşluk dedent).
- **Görünürlük:** public yüzey **birebir** — `mod.rs` `pub use {event::TurnStreamEvent, session::PersistentSession}` ile `swarm::persistent_session::{…}` yolunu yeniden yayımlıyor; `swarm/mod.rs:36 pub mod persistent_session;` + `:74 pub use persistent_session::PersistentSession;` ve tüketiciler (`agent_registry/registry.rs:14`, `agent_registry/slot.rs:3` — ikisi de `crate::swarm::persistent_session::{PersistentSession, TurnStreamEvent}` doğrudan-yol) **değişmedi**. Submodül-paylaşımlı item'lar `pub(super)`: `read::{read_until_result, drain_post_cancel, InvokeAccum}` (+ `InvokeAccum` alanları). `tests.rs` `use super::*` yerine açık import'larla çözülüyor (`super::read::InvokeAccum`, `super::PersistentSession`, `crate::swarm::transport::{classify_event, InvokeResult, StreamEvent}`, vb.). Davranış birebir.
- **Kanıt:** bypass'lı `cargo check --all-targets` → `Finished` exit 0, `persistent_session`'dan 0 tanı; bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (kullanılmayan import, bu refactor'la ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.

### T3-12 ☑ `swarm/transport.rs` (930L) — modül paketine bölündü
- **Etki:** en büyük kalan **stable** backend dosyasıydı; WP-W3-11 §4 one-shot `claude` subprocess transport — `StreamEvent` parser enum'u + `InvokeResult` wire çıktısı + `Transport` trait + durumlu `SubprocessTransport` (spawn/stdin-frame/stdout-read-loop/stderr-ring/timeout/persona-tmp) + saf stream-json line classifier (`classify_event` + 4 helper) + `RingBuffer`/`fmt_stderr_tail` + ~220L test tek dosyadaydı.
- **Yapıldı (2026-06-06_2100, DEEP):** `transport.rs` → `transport/` 4-üretim+1-test dosya paketi: `mod.rs` (modül docs + layout notu + re-export), `event.rs` (`StreamEvent` enum + `InvokeResult` struct), `classify.rs` (`TOOL_USE_INPUT_SUMMARY_CAP` + `classify_event` + `classify_assistant_blocks`/`classify_result_event`/`summarize_tool_input`/`truncate_with_ellipsis`), `ring.rs` (`STDERR_RING_CAPACITY` + `RingBuffer` + `fmt_stderr_tail`), `subprocess.rs` (`Transport` trait + `SubprocessTransport` + `InvokeAccum` + `write_persona_tmp` + stream-json contract docs), `tests.rs` (tüm test modülü).
- **Görünürlük:** public yüzey **birebir** — `mod.rs` `pub use event::InvokeResult; pub use subprocess::{SubprocessTransport, Transport};` + `pub(crate) use {classify::classify_event, event::StreamEvent, ring::{fmt_stderr_tail, RingBuffer, STDERR_RING_CAPACITY}, subprocess::write_persona_tmp};` ile `swarm::transport::{…}` yolunu yeniden yayımlıyor; `swarm/mod.rs:78 pub use transport::{InvokeResult, SubprocessTransport, Transport}` ve tüm tüketiciler (persistent_session/{read,session,tests}, agent_dispatcher/{invoke,invoker,tests}, agent_registry/registry, brain/{invoker,tests}, commands/swarm/{orchestrator,profiles}) **değişmedi**. `TOOL_USE_INPUT_SUMMARY_CAP` test-dışı tüketicisi olmadığından re-export edilmedi; `tests.rs` doğrudan `use super::classify::TOOL_USE_INPUT_SUMMARY_CAP` ile alıyor (re-export'sa test-dışı derlemede unused-import uyarısı doğardı). Submodül-içi `classify`/`event`/`ring` öğeleri `pub(crate)`, classify helper fn'leri private. Davranış birebir.
- **Kanıt:** bypass'lı `cargo check --all-targets` → `Finished` exit 0, `transport`'tan 0 tanı; bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (dangling import, bu refactor'la ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.

### T3-13 ☑ `sidecar/agent.rs` (816L) — modül paketine bölündü
- **Etki:** en büyük kalan **stable** backend dosyasıydı (test dosyaları hariç tüm hot-zone dışı üretim dosyalarının en büyüğü); WP-W2-04 LangGraph Python sidecar supervisor — `SidecarHandle`/`Inner` durumlu handle + public API (`start_run`/`shutdown`/`send`) + inbound/outbound wire shapes (`WireSpan`/`SidecarEvent`/`RunSpanPayload`/`SerializableWireSpan`) + `spawn_runtime`/`resolve_python` + stdout read-loop → DB write/emit (`read_loop`/`handle_event`/`insert_span`/`update_span`/`finalise_run`/`emit_span_event`) + ~240L test tek dosyadaydı. `sidecar/terminal.rs`→`terminal/` (T3-03) ile birebir simetrik; sidecar/ dizinini tutarlı hale getirir.
- **Yapıldı (2026-06-07_1400, DEEP):** `agent.rs` → `agent/` 4-üretim+1-test dosya paketi: `mod.rs` (modül docs/wiring + layout notu + `SidecarHandle`/`Inner` + public API impl `start_run`/`shutdown`/`send` + `from_inner` ctor + re-export), `wire.rs` (`WireSpan`/`SidecarEvent`/`RunSpanPayload`/`SerializableWireSpan` + manuel `Serialize` impl), `spawn.rs` (`spawn_runtime` + priv `resolve_python`), `reader.rs` (`read_loop` + priv `handle_event` + `insert_span`/`update_span`/`finalise_run` + priv `emit_span_event`), `tests.rs` (tüm test modülü). Ölü `_doc_only_path_use` hack'i + dangling `Path` import'u silindi (PathBuf artık yalnız spawn.rs'te).
- **Görünürlük:** public yüzey **birebir** — `mod.rs` `pub use spawn::spawn_runtime;` + `pub struct SidecarHandle` ile `crate::sidecar::agent::{SidecarHandle, spawn_runtime}` yolunu koruyor; tüketiciler (lib.rs:227 `spawn_runtime`, lib.rs:417 + commands/runs.rs:30 `SidecarHandle`) **değişmedi**. `from_inner` ctor `SidecarHandle.inner` alanını private tutmak için eklendi; `Inner` `pub(super)` (private_interfaces uyarısını gidermek için `from_inner` imzasıyla hizalı), alanları private kalır (spawn.rs descendant olduğundan erişir). Submodül-paylaşımlı item'lar `pub(super)`: `wire::*`, `reader::{read_loop, insert_span, update_span, finalise_run}`. `resolve_python` priv (gerçek kod tüketicisi yok — yalnız doc atıfları), `handle_event`/`emit_span_event` priv (yalnız reader.rs içi). `tests.rs` `use super::*` yerine açık import'larla (`super::reader::{finalise_run, insert_span, update_span}`, `super::spawn::spawn_runtime`, `super::wire::{SidecarEvent, WireSpan}`, `crate::test_support::{fresh_pool, seed_minimal}`) çözülüyor. Davranış birebir.
- **Kanıt:** bypass'lı `cargo check --manifest-path src-tauri/Cargo.toml --all-targets` → `Finished` exit 0, `sidecar/agent`'tan **0 tanı**; bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (dangling import, bu refactor'la ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.

### T3-14 ☑ `swarm/coordinator/orchestrator_session.rs` (675L) — modül paketine bölündü
- **Etki:** en büyük kalan **stable** (hot-zone dışı) üretim tek-dosyasıydı; WP-W3-12k2 kalıcı Orchestrator sohbet geçmişi — `OrchestratorMessageRole`/`OrchestratorMessage` wire-tipleri + SQLite write-through/read helper'ları (`append_user/orchestrator/job_message`, `list_recent_messages`, `clear_messages`) + prompt birleştirici (`render_with_history` + `action_label` + `HISTORY_TEMPLATE`) + ~300L test tek dosyadaydı.
- **Yapıldı (2026-06-07_2100, DEEP):** `orchestrator_session.rs` → `orchestrator_session/` 4-üretim+1-test dosya paketi: `mod.rs` (modül docs birebir + layout notu + re-export), `model.rs` (`OrchestratorMessageRole` enum + `pub(super) as_db_str`/`from_db_str` + `OrchestratorMessage` struct), `store.rs` (5 SQL helper), `render.rs` (`HISTORY_TEMPLATE` priv + `render_with_history` + `action_label` priv), `tests.rs` (tüm test modülü, açık import'lar).
- **Görünürlük:** public yüzey **birebir** — `mod.rs` `pub use model::{OrchestratorMessage, OrchestratorMessageRole}` + `pub(crate) use {render::render_with_history, store::{append_job_message, append_orchestrator_message, append_user_message, clear_messages, list_recent_messages}}` ile flat `orchestrator_session::{…}` yolunu yeniden yayımlıyor; `coordinator/mod.rs:35 pub use orchestrator_session::{OrchestratorMessage, OrchestratorMessageRole}` ve 2 tüketici (`commands/swarm/orchestrator.rs:19` 6-fn doğrudan-yol, `commands/swarm/tests.rs:22 append_user_message`) **değişmedi**. `as_db_str`/`from_db_str` private→`pub(super)` (model→store erişimi); cross-modül `orchestrator::{OrchestratorOutcome, OrchestratorAction}` atıfları `super::orchestrator`→`crate::swarm::coordinator::orchestrator` mutlak yola çevrildi. `tests.rs` `use super::*` yerine açık import'larla çözülüyor. Davranış birebir.
- **Kanıt:** bypass'lı `cargo check --all-targets` → `Finished` exit 0, `orchestrator_session`'dan **0 tanı**; bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (dangling import, bu refactor'la ilgisiz). text.rs `git checkout` ile birebir geri yüklendi.

### T3-15 ☑ `mcp/client.rs` (647L) — modül paketine bölündü
- **Etki:** en büyük kalan **stable** (hot-zone dışı) üretim tek-dosyasıydı; WP-W2-05 minimal MCP client — NDJSON JSON-RPC 2.0 transport. `tools/list`/`tools/call` wire-tipleri (`ToolDescriptor`/`CallToolOutput`/`ContentBlock`) + `MCP_PROTOCOL_VERSION` + JSON-RPC envelope'ları (`Request`/`Notification`/`Response`/`RpcError`) + durumlu `McpClient` (spawn/handshake/request/notify/list_tools/call_tool/ping/shutdown + NDJSON write/read) + ~200L test tek dosyadaydı.
- **Yapıldı (2026-06-08_1400, DEEP):** `client.rs` → `client/` 3-üretim+1-test dosya paketi: `mod.rs` (modül docs birebir + layout notu + re-export), `types.rs` (`MCP_PROTOCOL_VERSION` + `ToolDescriptor`/`CallToolOutput`/`ContentBlock` domain tipleri), `rpc.rs` (4 JSON-RPC envelope struct'ı, hepsi `pub(super)`), `connection.rs` (`McpClient` struct + tüm impl), `tests.rs` (tüm test modülü, açık import'lar).
- **Görünürlük:** public yüzey **birebir** — `mod.rs` `pub use connection::McpClient; pub use types::{CallToolOutput, ContentBlock, ToolDescriptor, MCP_PROTOCOL_VERSION}` ile `crate::mcp::client::{…}` yolunu yeniden yayımlıyor; `mcp/mod.rs:23 pub mod client;` (dizin modülünü çözer, değişmedi) ve 2 tüketici (`mcp/registry.rs:42,308` `crate::mcp::client::{CallToolOutput, McpClient, ToolDescriptor}`, `commands/mcp.rs:124-125` `crate::mcp::client::ContentBlock::{Text, Other}`) **değişmedi**. RPC envelope struct'ları + alanları private→`pub(super)` (connection.rs ve tests.rs sibling submodüllerinin construct/read'i için). `tests.rs` `use super::*` yerine açık `use super::{rpc::{Request, Response}, types::{…}}` import'larıyla çözülüyor. Davranış birebir; tek ek: test'teki 2 gereksiz `mut server_r` (orijinalde de pre-existing `unused_mut` uyarısıydı) kaldırıldı → paket 0-uyarı.
- **Kanıt:** bypass'lı `cargo check --all-targets` → `Finished` exit 0, `mcp/client`'tan **0 tanı**; bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (dangling import, bu refactor'la ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.

### T3-16 ☑ `models.rs` (503L) — domain submodül paketine bölündü
- **Etki:** IPC sınırının merkezî DTO dosyasıydı; 24 specta `Type` struct/enum (Agent/Workflow/Run/MCP/Pane/Mailbox/Me/Profile domain'leri) tek düz dosyada. En büyük kalan **stable** üretim tek-dosyalarından biri (binding.rs 543L'den sonra), saf veri (logic/test yok) → en temiz split adayı, sıfır davranış riski.
- **Yapıldı (2026-06-08_2100, DEEP):** `models.rs` → `models/` 8-domain+mod paketi: `mod.rs` (modül docs **birebir** + layout notu + flat re-export), `agent.rs` (Agent/AgentCreateInput/AgentPatch), `workflow.rs` (Workflow/Node/Edge/WorkflowDetail), `run.rs` (Run/RunFilter/Span/RunDetail), `mcp.rs` (Server/Tool/ToolContent/CallToolResult), `pane.rs` (Pane/ApprovalBanner/PaneSpawnInput/PaneLine), `mailbox.rs` (MailboxEntry/MailboxEntryInput), `me.rs` (User/Workspace/Me), `profile.rs` (ProfileSummary). Her submodül yalnız kullandığı import'u taşır (`serde::{Deserialize,Serialize}` + `specta::Type`; `sqlx::FromRow`/`std::collections::HashMap`/`crate::swarm::profile::PermissionMode` tam-yol).
- **Görünürlük:** public yüzey **birebir** — `mod.rs` `pub use {agent::{…}, …, workflow::{…}}` ile `crate::models::<Type>` flat yolunu yeniden yayımlıyor; `lib.rs:31 pub mod models;` dizin modülünü çözer (değişmedi) ve **26 tüketici referansının tamamı** `crate::models::<Type>` flat (alt-modül yolu kullanan yok) olduğundan **hiçbiri değişmedi**. Submodüller arası tip atfı yok → tümü `pub` kaldı, `pub(super)` gerekmedi. Davranış birebir.
- **Wire-shape / bindings:** struct adları + serde rename'leri + `#[serde(tag/rename_all)]`'ler birebir taşındı → `bindings.ts` **byte-identik** (tek doc-comment sapması yakalandı ve orijinaline geri alındı). **Kanıt:** `cargo run --bin export-bindings` sonrası `git diff -- app/src/lib/bindings.ts` = **0 satır** (içerik HEAD ile aynı; status'taki `M` yalnız autocrlf stat-dirty). `cargo check --all-targets` → `Finished` exit 0, **0 uyarı**.

### T3-04 ☐ refactor-v1 ertelenenleri — **hâlâ açık mı doğrula**
Repo W2'den W6'ya ilerledi; bu maddeler kapanmış olabilir. Her turda önce **var/yok teyidi**:
- `Supervisor` trait soyutlaması (C1) — `agent.rs` + `terminal.rs` ortak supervisor. ⏸ üçüncü sidecar gelince. **Not (2026-06-07_1400):** her iki dosya da artık modül paketi (`agent/` T3-13, `terminal/` T3-03); ortak supervisor trait'i ileride `agent/mod.rs::SidecarHandle` + `terminal/mod.rs::TerminalRegistry` üzerinden çıkarılabilir.
- MCP session pool + pending request map (C5) — `mcp/client/` paketi (T3-15 ile bölündü; `McpClient` artık `connection.rs`'te). ⏸ ayrı WP (davranışsal: session-pool + pending-request-map). Yapısal split tamam.
- Ortak `Status` enum (C2) — terminal vs run status ayrı enum'lar.
- Seeds modülü konsolidasyonu (D2).
- Capabilities daraltma (E1) — `tauri.conf.json` komut yüzeyi sabitlenince.
- Repo-level pre-commit hook (E2) — Co-Author trailer + gate; `core.hooksPath`/Husky ADR'ı ile.
- **Mod:** her biri audit + (küçükse) apply.

### T3-05 ☐ `tracing` / yapılandırılmış log denetimi
- **Etki:** refactor-v1 §⑨ `tracing` adopt etmişti; W3-W6 yeni kodda kaçak `eprintln!`/`println!` var mı?
- **Risk:** düşük. **Mod:** apply.

---

## 🔥 Hot zone — W6 aktif (snapshot'ta dirty → AUDIT-ONLY, patch yok)

> Bu alan ~17 gündür commit edilmemiş W6 (PTY router → dosya-tabanlı JSON inbox bridge) işidir. **Patch üretme** — yalnız bulgu + öneri. Kullanıcı W6'yı commit edince stable zone'a düşer.

- 🔥 `src-tauri/src/swarm_term/bridge.rs` (987L), `lifecycle.rs` (744L), `session.rs`, `persona.rs`, `home_isolation.rs`, `hierarchy.rs`
- 🔥 `app/src/routes/TerminalSwarm.tsx` (511L), `app/src/hooks/useTerminalSwarmSession.ts`, `useRoutingEvents.ts`, `app/src/styles/swarm-term.css`
- 🔥 9 persona doc'u (`src-tauri/src/swarm/agents/term/*.md`)
- **W6-02 izi** (`WP-W6-01-bridge-routing.md:62`): `useRoutingEvents.ts:6` stale yorum, design-parity audit, commit hijyeni. → audit notu.
- 🔥 **App.tsx:1-5 stale başlık yorumu (T1-03 doğrulandı):** "Routes are stubs ('coming soon')" diyor; oysa `App.tsx:12-21` 9 route'un tamamını gerçek bileşene bağlıyor. Stub kalmadı. App.tsx dirty → audit. W6 commit'lenince yorumu güncelle.
- 🔥 **App.test.tsx:234 stale assertion:** "renders the sidebar with all **7** nav items" — `NAV` artık **9** giriş (canvas, terminal, terminal-swarm, swarm, agents, runs, mcp, routing-log, settings). Test muhtemelen alt-küme dolaşıyor; sayı etiketi yanıltıcı. App.test.tsx dirty → audit.
- 🔥 **A11y (T1-07) doğrulandı:** `App.tsx:92-103` sidebar `li` yalnız `onClick` — `role`/`tabIndex`/klavye yok; `App.tsx:76` `.sb-brand` `role="button"` ama klavye handler'ı yok. Dirty → audit.

---

## Çözülenler (turlar buraya taşır)

- **T3-18 (mcp/registry)** (2026-06-09_2100, DEEP): `mcp/registry.rs` (540L) → `registry/` 3-üretim+1-test dosya paketi (`mod`/`spawn`/`store` + `tests`), `mcp/client/` (T3-15) ile simetrik. En büyük kalan **stable** (hot-zone dışı) üretim tek-dosyasıydı; WP-W2-05 install/uninstall/call orkestrasyonu. Üretim kodu (~300L) sorumluluk eksenine bölündü: `mod.rs` yalnız public lifecycle API'yi (orkestratörler `install`/`uninstall`/`list_tools`/`call_tool` + modül docs birebir + layout notu) taşıyor; manifest→connected-`McpClient` spawn çözümü (`fetch_tools`/`spawn_for_manifest` `pub(super)` + priv `resolve_command` `__ROOT__` substitution + priv `resolve_app_data_dir` + priv `resolve_env` secret-env + `pub(super) npx_executable`) `spawn.rs`'e, tüm `servers`/`server_tools` SQL'i (`persist_install` tx + `uninstall_server` flag-flip + `read_installed_flag` probe + `read_tools` read, hepsi `pub(super)`) `store.rs`'e ayrıldı; ~237L test `tests.rs`'e. **Davranış birebir:** mod.rs'in eski inline SQL'i (uninstall DELETE+UPDATE, call_tool'un `SELECT installed` ön-kontrolü, list_tools SELECT) store.rs helper'larına **birebir** taşındı — sorgu string'leri + hata mesajları (`Server {id} not found` / `Server {id} is not installed` / `Manifest {id} not found` / `Server {} not seeded`) byte-identik; `?`-üzerinden `From<sqlx::Error>` dönüşümü korundu. Public yüzey re-export'suz **birebir** — mod.rs 4 public fn'i aynen tutuyor; `mcp/mod.rs:25 pub mod registry;` dizin modülünü çözer (değişmedi), tek kod tüketici `commands/mcp.rs:55,70,84,115` (`registry::{install,uninstall,list_tools,call_tool}` `use crate::mcp::registry;` üzerinden) **değişmedi**. Submodül item'ları `pub(super)`; `resolve_command`/`resolve_app_data_dir`/`resolve_env` priv (yalnız spawn.rs-içi). `tests.rs` `use super::*` yerine açık import'larla (`super::{install, list_tools, uninstall}`, `super::spawn::npx_executable`, `super::store::persist_install`, `crate::mcp::client::{McpClient, ToolDescriptor}`, `crate::mcp::manifests`, `crate::db::DbPool`, `crate::test_support::fresh_pool`, `serde_json::json`, `std::collections::HashMap`) çözülüyor. Not: `secrets.rs:20,39,84` doc-comment'lerindeki `mcp::registry::resolve_env` düz-metin (intra-doc link değil) atfı artık `spawn::resolve_env`'i imliyor — derleme etkisi yok, descriptive prose olduğundan dokunulmadı. Wire-shape etkisi yok (registry IPC/specta `Type` içermez → `bindings.ts` ilgisiz). **Kanıt:** `cargo check --manifest-path src-tauri/Cargo.toml --all-targets` = `Finished` exit 0, **0 uyarı/hata** (W6 `reader.rs` kırığı bu snapshot'ta zaten giderilmiş, bypass gerekmedi). **Sonraki DEEP adayları (en büyük kalan stable üretim tek-dosyaları):** `commands/mailbox.rs` (522L), `commands/runs.rs` (497L), `db.rs` (495L), `lib.rs` (502L, setup/bootstrap — riskli, en sona).
- **T3-17 (swarm/binding)** (2026-06-09_1451, DEEP): `swarm/binding.rs` (543L) → `binding/` 4-üretim+1-test dosya paketi (`mod`/`resolve`/`env`/`args` + `tests`). En büyük kalan **stable** (hot-zone dışı) üretim tek-dosyasıydı; WP-W3-11 §3 `claude` CLI invocation helper'ları. Dosyanın kendi doc-comment'i zaten 3 sorumluluk listeliyordu → 1:1 submodüle bölündü: binary/spawn resolution (`ClaudeBinary`/`ClaudeSpawn` struct'ları + `resolve_claude_spawn` Windows .cmd→native .exe/cli.js bypass + `resolve_claude_binary` 3-adım env/which/fallback chain + `CLAUDE_BIN_ENV` + priv `platform_fallback_paths`/`home_dir`) `resolve.rs`'e, auth-routing env strip (`STRIPPED_ENV_VARS` 15-girişli sabit + `subscription_env`) `env.rs`'e, argv builder (`build_specialist_args` WP §3 flag-order contract) `args.rs`'e; ~190L test `tests.rs`'e. Public yüzey re-export'la birebir: `mod.rs` `pub use args::build_specialist_args; pub use env::subscription_env; pub use resolve::{resolve_claude_binary, resolve_claude_spawn, ClaudeBinary, ClaudeSpawn, CLAUDE_BIN_ENV}; pub(crate) use env::STRIPPED_ENV_VARS;` → flat `crate::swarm::binding::{…}` yolu korundu; `swarm/mod.rs:31 pub mod binding;` dizin modülünü çözer (değişmedi), `:44 pub use binding::{build_specialist_args, resolve_claude_binary, subscription_env, ClaudeBinary}` ve tüm tüketiciler (`transport/subprocess.rs:46-47,140`, `persistent_session/session.rs:19-20,83`, `commands/swarm_term.rs:19`, `swarm_term/session.rs:32`, `sidecar/terminal/tests.rs:358` — hepsi `crate::swarm::binding::{…}` doğrudan-yol veya `crate::swarm::{…}` re-export) **değişmedi**. `STRIPPED_ENV_VARS` orijinal `pub(crate)` görünürlüğünü korudu (mod.rs `pub(crate) use`); `platform_fallback_paths`/`home_dir` priv (yalnız resolve.rs-içi). Submodüller arası tip atfı yok → resolve/env/args tümü bağımsız. `tests.rs` `use super::*` yerine açık `use super::{build_specialist_args, subscription_env, STRIPPED_ENV_VARS}` + `crate::swarm::profile::{PermissionMode, Profile}` + `std::path::{Path, PathBuf}` import'larıyla çözülüyor. Davranış birebir. **Kanıt:** `cargo check --manifest-path src-tauri/Cargo.toml --all-targets` = `Finished` exit 0, **0 uyarı/hata** (binding'den 0 tanı; W6 `reader.rs` kırığı bu snapshot'ta zaten giderilmiş, bypass gerekmedi). Wire-shape etkisi yok (binding IPC/specta `Type` içermez → `bindings.ts` ilgisiz).
- **T3-16 (models)** (2026-06-08_2100, DEEP): `models.rs` (503L) → `models/` 9-dosya domain paketi (`mod`/`agent`/`workflow`/`run`/`mcp`/`pane`/`mailbox`/`me`/`profile`). IPC sınırının merkezî DTO dosyası; 24 specta `Type` struct/enum domain eksenine bölündü, `mod.rs` flat `pub use` ile `crate::models::<Type>` yolunu birebir koruyor → 26 tüketici referansı + `lib.rs:31 pub mod models;` değişmedi. Submodüller arası tip atfı yok (tümü `pub`). Wire-shape birebir korundu: `cargo run --bin export-bindings` sonrası `bindings.ts` byte-identik (bir doc-comment sapması yakalanıp orijinaline geri alındı). **Kanıt:** `cargo check --all-targets` = `Finished` exit 0, 0 uyarı; `git diff -- app/src/lib/bindings.ts` = 0 satır içerik drift'i. **Sonraki DEEP adayları (en büyük kalan stable üretim tek-dosyaları):** `mcp/registry.rs` (540L, MCP install/spawn — `mcp/client/` ile simetrik bölünebilir), `commands/mailbox.rs` (522L), `commands/runs.rs` (497L), `db.rs` (495L), `lib.rs` (502L, setup/bootstrap — riskli, en sona). (`swarm/binding.rs` 543L → T3-17'de bölündü.)
- **T3-15 (mcp/client)** (2026-06-08_1400, DEEP): `mcp/client.rs` (647L) → `client/` 4-dosya paketi (`mod`/`types`/`rpc`/`connection` + `tests`). En büyük kalan **stable** (hot-zone dışı) üretim tek-dosyasıydı. Üretim kodu (~444L) sorumluluk eksenine bölündü: `tools/list`/`tools/call` domain wire-tipleri (`ToolDescriptor` camelCase + `CallToolOutput` + `ContentBlock` `#[serde(tag="type")]` + `MCP_PROTOCOL_VERSION` pin) `types.rs`'e, NDJSON JSON-RPC envelope'ları (`Request`/`Notification` serialize + `Response`/`RpcError` deserialize) `rpc.rs`'e, durumlu `McpClient` (spawn+handshake `initialize`/`notifications/initialized`, `request` id-correlation/timeout/notification-skip loop, `notify`, `list_tools`/`call_tool` typed conveniences, `ping`, `shutdown` EOF+kill-race, `write_line`/`read_line` NDJSON codec) `connection.rs`'e; ~200L test `tests.rs`'e. Public yüzey re-export'la birebir: `mod.rs` `pub use connection::McpClient; pub use types::{CallToolOutput, ContentBlock, ToolDescriptor, MCP_PROTOCOL_VERSION}` → `crate::mcp::client::{…}` ve 2 tüketici (`mcp/registry.rs:42,308`, `commands/mcp.rs:124-125`) değişmedi; `mcp/mod.rs:23 pub mod client;` dizin modülünü çözer, değişmedi. RPC envelope struct'ları + alanları private→`pub(super)` (sibling `connection`/`tests` construct/read erişimi). `tests.rs` `use super::*` yerine açık `use super::rpc::{Request, Response}` + `use super::types::{CallToolOutput, ContentBlock, ToolDescriptor, MCP_PROTOCOL_VERSION}` + `serde_json::{json, Value}` + tokio io trait'leriyle çözülüyor. Pre-existing 2 `unused_mut` (`mut server_r`) temizlendi → paket 0-uyarı. Davranış birebir. **Kanıt:** bypass'lı `cargo check --all-targets` = `Finished` exit 0 (mcp/client'tan 0 tanı); bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (dangling import, ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.
- **T3-14 (orchestrator_session)** (2026-06-07_2100, DEEP): `swarm/coordinator/orchestrator_session.rs` (675L) → `orchestrator_session/` 5-dosya paketi (`mod`/`model`/`store`/`render` + `tests`). En büyük kalan **stable** (hot-zone dışı) üretim tek-dosyasıydı. Üretim kodu (~368L) sorumluluk eksenine bölündü: chat-message wire-tipleri (`OrchestratorMessageRole` snake_case enum + `OrchestratorMessage` camelCase struct) `model.rs`'e, SQLite write-through/read helper'ları (`append_user_message`/`append_orchestrator_message`/`append_job_message` INSERT'leri + `list_recent_messages` DESC-then-reverse + `clear_messages` DELETE) `store.rs`'e, prompt birleştirici (`render_with_history` per-role line-grain format + `action_label` + `HISTORY_TEMPLATE`) `render.rs`'e; ~300L test `tests.rs`'e taşındı. Public yüzey re-export'la birebir korundu: `mod.rs` `pub use model::{OrchestratorMessage, OrchestratorMessageRole}` + `pub(crate) use {render::render_with_history, store::{append_job_message, append_orchestrator_message, append_user_message, clear_messages, list_recent_messages}}` → `coordinator/mod.rs:35 pub use orchestrator_session::{OrchestratorMessage, OrchestratorMessageRole}` ve 2 tüketici (`commands/swarm/orchestrator.rs:19` 6 helper doğrudan-yol, `commands/swarm/tests.rs:22 append_user_message`) değişmedi. `OrchestratorMessageRole::as_db_str`/`from_db_str` private→`pub(super)` (model→store); render/store içindeki sibling `super::orchestrator::*` atıfları `crate::swarm::coordinator::orchestrator::{OrchestratorOutcome, OrchestratorAction}` mutlak yola çevrildi. `tests.rs` `use super::*` yerine açık import'larla (`super::{append_*, clear_messages, list_recent_messages, render_with_history, OrchestratorMessage, OrchestratorMessageRole}`, `crate::swarm::coordinator::orchestrator::{OrchestratorAction, OrchestratorOutcome}`, `crate::test_support::fresh_pool`) çözülüyor. Davranış birebir. **Kanıt:** bypass'lı `cargo check --all-targets` = `Finished` exit 0 (orchestrator_session'dan 0 tanı); bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (dangling import, ilgisiz). text.rs `git checkout` ile birebir geri yüklendi.
- **T3-13 (sidecar/agent)** (2026-06-07_1400, DEEP): `sidecar/agent.rs` (816L) → `agent/` 5-dosya paketi (`mod`/`wire`/`spawn`/`reader` + `tests`), `sidecar/terminal.rs`→`terminal/` (T3-03) ile simetrik. Üretim kodu (~575L) sorumluluk eksenine bölündü: durumlu `SidecarHandle`/`Inner` + public API (`start_run`/`shutdown`/`send` JSON-RPC frame writer) `mod.rs`'te, inbound/outbound wire shapes (`WireSpan` camelCase span + `SidecarEvent` `#[serde(tag="event")]` envelope + `RunSpanPayload` + `SerializableWireSpan` manuel `Serialize`) `wire.rs`'e, `spawn_runtime` (python -m agent_runtime spawn + kill_on_drop + PYTHONUNBUFFERED + read-loop task) + `resolve_python` (NEURON_AGENT_PYTHON→.venv→PATH) `spawn.rs`'e, stdout read-loop → DB writer/emitter (`read_loop`/`handle_event`/`insert_span`/`update_span`/`finalise_run`/`emit_span_event`) `reader.rs`'e ayrıldı; ~240L test `tests.rs`'e. Ölü `_doc_only_path_use` hack'i + dangling `std::path::Path` import'u silindi. Public yüzey re-export'la birebir: `mod.rs` `pub use spawn::spawn_runtime;` + `pub struct SidecarHandle` → `crate::sidecar::agent::{SidecarHandle, spawn_runtime}` ve tüketiciler (lib.rs:227/417, commands/runs.rs:30) değişmedi. `SidecarHandle.inner` alanı private kaldı (`from_inner` ctor üzerinden spawn.rs inşa ediyor); `Inner` `pub(super)` (private_interfaces uyarı hizalaması). Submodül-paylaşımlı: `wire::*` + `reader::{read_loop, insert_span, update_span, finalise_run}` `pub(super)`; `resolve_python`/`handle_event`/`emit_span_event` private. `tests.rs` açık import'larla (`use super::reader::{…}`, `super::spawn::spawn_runtime`, `super::wire::{…}`, `crate::test_support::{…}`) çözülüyor. Davranış birebir. **Kanıt:** bypass'lı `cargo check --all-targets` = `Finished` exit 0 (sidecar/agent'tan 0 tanı); bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (dangling import, ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.
- **T3-12 (transport)** (2026-06-06_2100, DEEP): `swarm/transport.rs` (930L) → `transport/` 5-dosya paketi (`mod`/`event`/`classify`/`ring`/`subprocess` + `tests`). Üretim kodu sorumluluk eksenine bölündü: saf data tipleri (`StreamEvent` parser enum + `InvokeResult` camelCase wire çıktısı) `event.rs`'e, saf-sync stream-json line classifier (`classify_event` + `classify_assistant_blocks`/`classify_result_event`/`summarize_tool_input`/`truncate_with_ellipsis` + `TOOL_USE_INPUT_SUMMARY_CAP`) `classify.rs`'e, tail-only stderr `RingBuffer` + `STDERR_RING_CAPACITY` + `fmt_stderr_tail` `ring.rs`'e, durumlu spawn/drive tarafı (`Transport` trait + `SubprocessTransport` + `invoke` spawn/stdin-NDJSON/stdout-read-loop+timeout/stderr-ring-drain/persona-tmp-cleanup + `InvokeAccum` + `write_persona_tmp` + tam stream-json contract docs) `subprocess.rs`'e; ~220L test `tests.rs`'e taşındı. Public yüzey re-export'la birebir korundu: `mod.rs` `pub use event::InvokeResult; pub use subprocess::{SubprocessTransport, Transport};` + `pub(crate) use {classify::classify_event, event::StreamEvent, ring::{fmt_stderr_tail, RingBuffer, STDERR_RING_CAPACITY}, subprocess::write_persona_tmp};` → `swarm/mod.rs:78 pub use transport::{InvokeResult, SubprocessTransport, Transport}` ve tüm tüketiciler (persistent_session/{read,session,tests} `crate::swarm::transport::{classify_event, fmt_stderr_tail, write_persona_tmp, InvokeResult, RingBuffer, StreamEvent, STDERR_RING_CAPACITY}`, agent_dispatcher/{invoke,invoker,tests}, agent_registry/registry, brain/{invoker,tests} `…::InvokeResult`, commands/swarm/{orchestrator,profiles} `SubprocessTransport`) değişmedi. `TOOL_USE_INPUT_SUMMARY_CAP` test-dışı tüketicisi olmadığından mod.rs'de re-export edilmedi; `tests.rs` `use super::classify::TOOL_USE_INPUT_SUMMARY_CAP` + `use super::*` + `serde_json::Value`/`std::time::Duration` açık import'larıyla çözülüyor. Submodül-içi öğeler orijinal `pub(crate)` görünürlüğünü korudu; classify helper fn'leri private. Davranış birebir. **Kanıt:** bypass'lı `cargo check --all-targets` = `Finished` exit 0 (transport'tan 0 tanı); bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (dangling import, bu refactor'la ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.
- **T3-11 (persistent_session)** (2026-06-06_1400, DEEP): `swarm/persistent_session.rs` (991L) → `persistent_session/` 5-dosya paketi (`mod`/`event`/`read`/`session` + `tests`). Üretim kodu (~568L) sorumluluk eksenine bölündü: W4-03'e teslim edilen hot-path streaming enum (`TurnStreamEvent` + agent_registry'ye dep-asiklik notu) `event.rs`'e, stdout read-loop (`read_until_result` cancel/timeout select + stream-json classify, `drain_post_cancel` byte/wall-time-bütçeli post-cancel drain, `InvokeAccum` per-turn running-state, `POST_CANCEL_DRAIN_BUDGET_BYTES`/`POST_CANCEL_DRAIN_TIMEOUT` sabitleri) `read.rs`'e, durumlu `PersistentSession` (spawn argv/env-strip + stderr ring-drain task, `invoke_turn` stdin-frame/read/cancel-drain/turn-sayaç, `turns_taken`/`profile_id`, `shutdown` grace-window+kill, `Drop`, `dummy_child_stdin` mem::replace placeholder, `attach_stderr_tail`, `SHUTDOWN_GRACE_BUDGET`) `session.rs`'e; ~412L test `tests.rs`'e (4-boşluk dedent). Public yüzey re-export'la birebir korundu: `mod.rs` `pub use {event::TurnStreamEvent, session::PersistentSession}` → `swarm/mod.rs:36 pub mod persistent_session;` + `:74 pub use persistent_session::PersistentSession;` ve 2 tüketici (`agent_registry/registry.rs:14`, `agent_registry/slot.rs:3` — `crate::swarm::persistent_session::{PersistentSession, TurnStreamEvent}` doğrudan-yol) değişmedi. Submodül-paylaşımlı item'lar `pub(super)`: `read::{read_until_result, drain_post_cancel, InvokeAccum}` (+ `InvokeAccum.session_id`/`.assistant_text` alanları). `tests.rs` `use super::*` yerine açık import'larla (`super::read::InvokeAccum`, `super::PersistentSession`, `crate::error::AppError`, `crate::swarm::transport::{classify_event, InvokeResult, StreamEvent}`, `serde_json::Value`, `tokio::io::{AsyncBufReadExt, AsyncWriteExt, BufReader}`, `tokio::sync::Notify`, std `Arc`/`Duration`/`Write`) çözülüyor; helper `read_until_result_via_duplex*` üretim read-loop'unu birebir replikliyor. Davranış birebir. **Kanıt:** bypass'lı `cargo check --all-targets` = `Finished` exit 0 (persistent_session'dan 0 tanı); bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (kullanılmayan import, bu refactor'la ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.
- **T3-10 (profile)** (2026-06-05_2100, DEEP): `swarm/profile.rs` (1007L) → `profile/` 5-dosya paketi (`mod`/`types`/`registry`/`parser` + `tests`). Üretim kodu (~640L) sorumluluk eksenine bölündü: wire-tipleri (`PermissionMode` enum + frontmatter `parse`, `Profile` struct, `ProfileSource` enum/impl) `types.rs`'e, durumlu yükleyici (`BUNDLED_AGENTS` `include_dir!` static + `ProfileRegistry` + `load_from`/`load_term` bundled+workspace okuyucular + `get`/`source`/`list`/`profile_count`) `registry.rs`'e, elle yazılmış frontmatter parser (`id_regex`/`version_regex` `OnceLock` regex'leri + `parse_profile` delimiter-tarama/body-slice/kv-walk + `required` + `parse_allowed_tools` JSON/YAML-flow normalizasyon state-machine) `parser.rs`'e; ~363L test `tests.rs`'e taşındı. Public yüzey re-export'la birebir korundu: `mod.rs` `pub use {registry::ProfileRegistry, types::{PermissionMode, Profile, ProfileSource}}` → `swarm/mod.rs:75-77 pub use profile::{…}` ve 7 tüketici dosya (models.rs `crate::swarm::profile::{Profile, PermissionMode}` doğrudan-yol, binding.rs, agent_registry/{registry,tests}, persistent_session.rs, transport.rs, swarm_term/session.rs) değişmedi. Submodül-paylaşımlı item'lar `pub(super)`: `PermissionMode::parse` (types→parser/tests), `parse_profile`+`parse_allowed_tools` (parser→registry/tests). `tests.rs` açık `use super::{parser::{parse_allowed_tools, parse_profile}, registry::ProfileRegistry, types::{PermissionMode, ProfileSource}}` + `std::path::{Path, PathBuf}` + `tempfile::TempDir` import'larıyla çözülüyor. Davranış birebir. **Kanıt:** bypass'lı `cargo check --all-targets` = `Finished` exit 0 (profile'dan 0 tanı); bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (kullanılmayan import, bu refactor'la ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.
- **T3-09 (agent_registry)** (2026-06-05_1400, DEEP): `swarm/agent_registry.rs` (1029L) → `agent_registry/` 7-dosya paketi (`mod`/`status`/`event`/`config`/`slot`/`registry` + `tests`). Üretim kodu (~675L) sorumluluk eksenine bölündü: read-only status wire (`AgentStatus` enum + `AgentStatusRow`) `status.rs`'e, W4-03 event channel (`SwarmAgentEvent` tagged-enum + `agent_event_channel` ad-üretici) `event.rs`'e, `NEURON_SWARM_AGENT_TURN_CAP` çözümü (`DEFAULT_TURN_CAP`/`TURN_CAP_ENV` sabitleri + `resolve_turn_cap`) `config.rs`'e, internal per-agent `AgentSlot` (+ `new`) `slot.rs`'e, durumlu `SwarmAgentRegistry` (RwLock session-map + per-agent `Mutex<AgentSlot>` + W5-02 dispatcher-map; `acquire_and_invoke_turn` lazy-spawn/turn-cap-respawn/forwarder, `list_status`, `shutdown_workspace`, `shutdown_all`, `ensure_dispatcher`, diagnostik fn'ler) `registry.rs`'e; ~350L test `tests.rs`'e taşındı. Public yüzey re-export'la birebir korundu: `mod.rs` `pub use {event::{agent_event_channel, SwarmAgentEvent}, registry::SwarmAgentRegistry, status::{AgentStatus, AgentStatusRow}}` → `swarm/mod.rs:65-68 pub use agent_registry::{…}` ve 6 tüketici dosya (lib.rs, commands/swarm/{agents,dispatch,run,tests}, brain/invoker.rs, agent_dispatcher/invoker.rs) değişmedi; `crate::swarm::agent_registry::SwarmAgentRegistry` doğrudan-yol atıfları korundu. Submodül-paylaşımlı item'lar `pub(super)`: `config::{resolve_turn_cap, DEFAULT_TURN_CAP, TURN_CAP_ENV}`, `slot::AgentSlot` + 4 alanı (registry'nin doğrudan alan erişimi için). `tests.rs` `use super::*` (re-export'lar) + açık `use super::config::{…}` + `crate::swarm::profile::ProfileRegistry`/`Arc`/`Notify`/`Duration` import'larıyla çözülüyor. Davranış birebir. **Kanıt:** bypass'lı `cargo check --all-targets` = `Finished` exit 0 (agent_registry'den 0 tanı); bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (text.rs'te yok, dangling import — bu refactor'la ilgisiz). text.rs `git checkout` ile birebir geri yüklendi.
- **T3-08 (coordinator/store)** (2026-06-04_1457, DEEP): `swarm/coordinator/store.rs` (1292L) → `coordinator/store/` 5-dosya paketi (`mod`/`cols`/`write`/`read`/`recovery` + `tests`). Üretim kodu (~542L) sorumluluk eksenine bölündü: JSON kolon kodek'leri + goal-truncation `cols.rs`'e (`serialize/deserialize_verdict`, `serialize/deserialize_decision`, `truncate_chars`), job/stage/lock INSERT-UPDATE write-through `write.rs`'e, recent-jobs list + job detail + stage rows + full-`Job` hydration `read.rs`'e, process-start orphan sweep + `RecoveredOrphans` `recovery.rs`'e; ~747L test `tests.rs`'e (4-boşluk dedent + lokal type import'lar zaten test fn'leri içinde, ek olarak `truncate_chars`/`Job`/`JobState`/`StageResult` top-level import). Public yüzey birebir: orijinal `pub(super)` (=coordinator) → coordinator-facing fn'ler `pub(in crate::swarm::coordinator)`, mod.rs `pub(super) use` ile `store::{insert_job_and_lock, update_job, insert_stage, delete_workspace_lock, list_jobs, get_job_detail, list_recent_jobs_full, recover_orphans}` flat yolunu yeniden yayımlıyor. `RecoveredOrphans` re-export edilmedi (tek tüketici `registry.rs` tipi adlandırmıyor, yalnız `.count`/`.recovered` alanlarına erişiyor; tip zaten coordinator-görünür). `coordinator/mod.rs:24 pub(crate) mod store;` bildirimi dizin modülünü de çözer, değişmedi. Tek kod tüketici `coordinator/job/registry.rs` (hot-zone, dokunulmadı) değişmedi; projector'daki `coordinator::store::*` atıfları yalnızca doc-comment. Davranış birebir. **Kanıt:** bypass'lı `cargo check --all-targets` = `Finished` exit 0 (store'dan 0 tanı); bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (kullanılmayan import, bu refactor'la ilgisiz). reader.rs `git checkout` ile birebir geri yüklendi.
- **T3-07 (mailbox_bus)** (2026-06-03_2100, DEEP): `swarm/mailbox_bus.rs` (1458L) → `mailbox_bus/` 5-dosya paketi (`mod`/`event`/`envelope`/`bus`/`tests`). `MailboxEvent` tagged-enum (+ SQL `kind`/`payload_json` round-trip helper'ları) `event.rs`'e, `MailboxEnvelope` wire-shape `envelope.rs`'e, durumlu `MailboxBus` (per-workspace `broadcast` kanalları + persist/broadcast/Tauri-emit `emit_typed`/`list_typed` + WP-W5-05 workspace-busy guard + `cancel_in_flight_brain_jobs` shutdown fan-out + `BROADCAST_CAPACITY`/`LIST_TYPED_*` sabitleri) `bus.rs`'e ayrıldı; ~820L test `tests.rs`'e taşındı. Public yüzey re-export'la birebir korundu: `swarm::mailbox_bus::{MailboxBus, MailboxEnvelope, MailboxEvent}` → `swarm/mod.rs:73` `pub use` ve 6 tüketici dosya (projector/*, brain/*, agent_dispatcher/*, commands/*, agent_registry.rs, lib.rs) değişmedi. `tests.rs` `use super::*` + açık `DbPool`/`AppError`/`MailboxEntry` import'larıyla aynen çözülüyor. Davranış birebir. **Kanıt:** bypass'lı `cargo check --all-targets` = `Finished` exit 0 (mailbox_bus 0 tanı); bypass'sız tek hata pre-existing hot-zone W6 `sidecar/terminal/reader.rs:29 display_safe_len` (bu refactor'la ilgisiz, kullanılmayan import). reader.rs `git checkout` ile birebir geri yüklendi.
- **T3-01 (test modülü)** (2026-06-03_1400, DEEP): `commands/swarm.rs` (1908L) → kök **82L**; ~1824L'lik `#[cfg(test)] mod tests` bloku `commands/swarm/tests.rs`'e taşındı (saf move + 4-boşluk dedent, diğer paketlerin `tests.rs` deseni). Komut alt-modülleri (agents/dispatch/jobs/orchestrator/profiles/run) ve `pub use` re-export'ları + `workspace_agents_dir` kökte kaldı. Test modülü `commands::swarm`'ın çocuğu olmaya devam ettiği için `use super::*;` aynı çözülüyor; davranış birebir. **Kanıt:** bypass'lı `cargo check --all-targets` = `Finished` (0 hata); bypass'sız tek hata pre-existing W6 hot-zone `sidecar/terminal/reader.rs:29 display_safe_len` (bu refactor'la ilgisiz, kullanılmayan import). reader.rs `git checkout` ile birebir geri yüklendi.
- **T3-06 (coordinator/job)** (2026-06-02_2100, DEEP): `swarm/coordinator/job.rs` (1471L) → `coordinator/job/` 6-dosya paketi (`mod`/`state`/`model`/`registry`/`event`/`tests`). Job domain-model tipleri (`StageResult`/`Job`/`JobOutcome`/`JobSummary`/`JobDetail`), `JobState` enum, durumlu `JobRegistry` (kilit + cancel-notify + SQL write-through), ve `SwarmJobEvent` ayrı sorumluluk dosyalarına ayrıldı. Public yüzey re-export'la birebir korundu (`coordinator::job::{…}` → `coordinator/mod.rs`, `store.rs`, `lib.rs`, `projector/*` tüketicileri değişmedi). `cargo check --all-targets`: coordinator/job'dan 0 tanı; tek hata pre-existing hot-zone W6 kırığı (`sidecar/terminal/reader.rs` `display_safe_len`), bu refactor'la ilgisiz.
- **T3-02 (agent_dispatcher kısmı)** (2026-06-02_0953, DEEP): `swarm/agent_dispatcher.rs` (2118L) → `agent_dispatcher/` 6-dosya paketi:
  `mod.rs` (modül docs + re-export + `MailboxAgentDispatcher` handle + `InvokeSlot`),
  `config.rs` (timeout sabitleri + `dispatch_timeout()`),
  `invoker.rs` (`AgentInvoker` trait + `SwarmAgentRegistryInvoker`),
  `routing.rs` (`parse_agent_target` + `run_loop` + `handle_envelope`),
  `invoke.rs` (`drive_invoke` + W5-03 help-loop + `wait_for_help_outcome`),
  `tests.rs` (tüm test modülü). Public yüzey aynen: `parse_agent_target`, `AgentInvoker`, `MailboxAgentDispatcher`, `SwarmAgentRegistryInvoker` (mod.rs:56-59 re-export'ları + `crate::swarm::agent_dispatcher::*` path'leri korundu). Görünürlük: cross-modül item'lar `pub(super)`. Davranış birebir. Kanıt: `cargo check --all-targets` → `Finished` (terminal/ W6 kırığı geçici by-pass edilip doğrulandı, sonra reader.rs byte-byte geri yüklendi).
- **T3-03** (2026-06-01_1400, DEEP): `sidecar/terminal.rs` (2187L) → `terminal/` 6-dosya paketi (mod/text/command/approval/reader/tests). Public yüzey korundu, `cargo check --all-targets` yeşil.
- **T3-01 / T3-02 (kısmi)** (önceki turlar): `commands/swarm.rs` 2878→1908L (`commands/swarm/{agents,dispatch,jobs,orchestrator,profiles,run}.rs`) ve `swarm/projector.rs` → `swarm/projector/{mod,helpers,outcome,persistence,projection,query,tests}.rs` bölündü.
