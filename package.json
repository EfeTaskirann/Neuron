{
  "name": "neuron",
  "version": "0.1.0",
  "private": true,
  "description": "Neuron — Agent Development Environment (Tauri 2 + React 18)",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "tauri dev",
    "dev:web": "pnpm --filter @neuron/app dev",
    "build": "tauri build",
    "build:web": "pnpm --filter @neuron/app build",
    "preview": "pnpm --filter @neuron/app preview",
    "typecheck": "pnpm --filter @neuron/app typecheck",
    "lint": "pnpm --filter @neuron/app lint",
    "test": "pnpm --filter @neuron/app test",
    "gen:bindings": "cargo run --manifest-path src-tauri/Cargo.toml --bin export-bindings",
    "gen:bindings:check": "cargo run --manifest-path src-tauri/Cargo.toml --bin export-bindings && git diff --exit-code app/src/lib/bindings.ts",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.1.0"
  }
}
